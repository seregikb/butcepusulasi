---
import PageIntro from '@/components/PageIntro.astro';
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  eyebrow?: string;
  noindex?: boolean;
  robots?: 'index, follow' | 'noindex, nofollow';
}

const { title, description, eyebrow, noindex = false, robots } = Astro.props;
---

<BaseLayout title={title} description={description} noindex={noindex} robots={robots}>
  <PageIntro {eyebrow} {title} {description} />
  <div class="container py-12 md:py-16">
    <div class="prose"><slot /></div>
  </div>
</BaseLayout>
